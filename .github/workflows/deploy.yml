name: Deploy nalewki to MyDevil

on:
  push:
    branches:
      - main

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build
        env:
          VITE_STRIPE_LINK_MINI: ${{ secrets.NALEWKI_STRIPE_LINK_MINI }}
          VITE_STRIPE_LINK_MIDI: ${{ secrets.NALEWKI_STRIPE_LINK_MIDI }}
          VITE_STRIPE_LINK_MAXI: ${{ secrets.NALEWKI_STRIPE_LINK_MAXI }}
          VITE_STRIPE_LINK_VIP: ${{ secrets.NALEWKI_STRIPE_LINK_VIP }}
          VITE_STRIPE_PUBLIC_KEY: ${{ secrets.VITE_STRIPE_PUBLIC_KEY }}

      - name: Deploy via SCP
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          source: "dist/*"
          target: "domains/nalewki.ebra.pl/public_html/"
          strip_components: 1
